/*
 * Shared — Type Definitions
 * ===========================
 * Cross-cutting types used by layout and navigation components.
 */

/** Navigation link type */
export interface NavLink {
  label: string;
  href: string;
}
